name: hello
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name to greet'
        required: true
        default: 'World'
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Greet
        run: echo "Hello, ${{ github.event.inputs.name }}!"
